import { Col, Row } from 'antd';
import React from 'react';

export const Grid = ({ span, children, spacing }) => {
  <Row>
    <Col span={12}>col-12</Col>
    <Col span={12}>col-12</Col>
  </Row>;
};
